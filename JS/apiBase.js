// Central API base URL for all frontend scripts

// Change this to your backend's base URL as needed
export const apiBase = window.location.protocol + "//" + window.location.hostname + ":5500";
export const apiBase2 = window.location.protocol + "//" + window.location.hostname;